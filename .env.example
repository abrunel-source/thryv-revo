# Contact form (Resend) — set these in Vercel → Project → Settings → Environment Variables.
# Copy this file to .env for local development (.env is git-ignored).

# Required: your Resend API key (restricted to the thryv.co.za sending domain).
RESEND_API_KEY=

# Optional overrides (defaults shown):
# CONTACT_TO=admin@thryv.co.za
# CONTACT_CC=abrunel@brunelstudios.com
# CONTACT_FROM=Thryv Website <noreply@thryv.co.za>
